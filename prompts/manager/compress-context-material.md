上下文材料（仅用于压缩，不要原样复述）：

# FocusId
{{focus_id}}

# ExistingCompressedContext
{{existing_compressed_context}}

# RecentHistory
{{recent_history}}

# TasksSnapshot
{{tasks_snapshot}}

提取优先级（高到低）：
1. RecentHistory 中已确认的决策与约束
2. TasksSnapshot 中已完成/失败/待执行状态
3. ExistingCompressedContext 中仍然有效且未被新信息覆盖的要点
