# Task Plan: Split TS Files Under 200 Lines

1. Audit `src` for .ts files over 200 lines and map refactor targets. ✓
2. Split each oversized module into smaller focused files and update imports. ✓
3. Re-count line lengths and iterate until all .ts files are <200 lines. ✓
