# notes_8419f128e6284c7381392b0aa88507c6

## 直接证据

- 任务合同引用的 `plans/report_75ac8392ee1045258ddbad196db5760b.md` 与 `tasks/...` 在当前 worktree 内不存在；真实可读文件位于 `/Users/mimiko/Projects/mimikit/` 主 worktree。
- 喷点报告把高优先级项按 `TTS / WebUI 控制面`、`plan.taskTemplate`、状态真相源、SSE、focus 横切排序；其中 TTS 被明确标为“最典型的跑题功能”。
- 当前分支直接代码证据集中在 `webui-src/hooks/use-tts.ts`、`webui-src/lib/tts-*.ts`、`webui-src/app-runtime/*`、`webui-src/components/Header.tsx`。

## 方案收敛

- 选择项：删除 WebUI TTS 全链路，并移除 Header/Tools 中的 `Voice replies` 入口。
- 不纳入本轮：
  - WebUI 其余控制面：`Reset / Restart / Delete messages` 仍直接服务在线操作面，本轮证据不足以一起删除。
  - `plan.effect.taskTemplate`：高 ROI，但会触及编排合同与持久化 schema，闭环比本轮更大。
  - 状态真相源 / SSE / focus：都需要先做只读边界澄清，当前不适合直接写。

## TDD 记录

- 先把 `tests/webui-react-header.test.ts` 改成断言 Header 不再渲染 `Voice replies` 与 `menuitemcheckbox`，并用 `toolsMenuOpen: true` 暴露当前行为。
- 红灯验证：`pnpm vitest run tests/webui-react-header.test.ts` 首次失败，错误点在 `Header.tsx` 仍访问 `ttsLabel.endsWith(...)`。
- 绿灯验证：删除 TTS 相关实现后，`pnpm vitest run tests/webui-react-header.test.ts tests/webui-react-messages-state.test.ts` 通过。

## 代码评审笔记

- 批次 1/2：`webui-src/app-runtime/*`、`webui-src/hooks/use-app-*`、`webui-src/components/Header.tsx`
  - ✓ 真实性：只收紧 TTS 入口与运行时链路，没有顺手动其他控制面。
  - ✓ 正确性：`pnpm run type-check` 通过；Header props、surface 类型、local action 参数已同步收口。
  - ✓ 优雅性：删除了 TTS 特例分支，没有留下“空壳 toggle”。
  - ✓ 最小化：`use-app-runtime-effects` 不再保留只为 TTS 服务的 hydration baseline 分支。
- 批次 2/2：`webui-src/lib/messages.ts`、删除 `webui-src/hooks/use-tts.ts` 与 `webui-src/lib/tts-*.ts`、测试更新
  - ✓ 真实性：只删除当前唯一路径使用的 TTS 工具代码。
  - ✓ 正确性：保留 `applyIncomingSnapshot(...).newAgentMessages`，避免误伤 `awaitingReply` 语义。
  - ✓ 优雅性：同步删掉失效测试与只为 TTS 服务的 helper export。
  - ✓ 最小化：净删除 500+ 行，无新增兼容层。
- 结论：未发现阻塞合并的问题。
