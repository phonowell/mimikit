# task_plan_f16f7f4063e64457ae616d38660ec23a

- [x] 1. 读取外置任务合同与两份直接相关归档，复核当前 `main` / feature / task worktree 的真实状态。
- [x] 2. 盘点 `main` 与 `task/webui-surface-context-f8f964d4c7` 的直接冲突文件，确认仅限 WebUI surface/context 受控边界。
- [x] 3. 在当前 task worktree 上集成最新 `main`，解决与 surface/context 收敛直接相关的 merge 冲突并保持边界最小。
- [x] 4. 运行最小必要验证与质量门禁；若失败则只修正本次集成直接引入的问题。
- [ ] 5. 将已验证结果 merge back 到 `/Users/mimiko/Projects/mimikit` 的 `main`，并清理 `task/webui-surface-context-f8f964d4c7` 对应 worktree/branch。
- [ ] 6. 落盘归档、计划备注与最终交接，明确证据、风险与 git 生命周期。

## 当前状态

- 类型：implementation
- 当前阶段：验证已通过，准备提交当前 merge 结果并回写到 `main`。
- 已确认事实：`/Users/mimiko/Projects/mimikit` 的 `main` 当前干净，HEAD 为 `a3061d42`；当前 task worktree 位于 `ab0e436e`；feature worktree 位于 `32a76723` 且仍保留。
- 已落地策略：保留 `main` 上的 copy-feedback surface/state 语义，同时采用 feature 的 `AppRuntime -> AppRuntimeShell` 直连结构，删除四个中转 provider/context 文件。
- 验证结果：`pnpm test -- tests/webui-react-app-runtime-shell.test.ts tests/webui-react-plans-dialog.test.ts tests/webui-react-tasks-dialog.test.ts` 与 `pnpm review-code-changes` 均已通过。
- 受控边界：仅处理 `webui-src/app-runtime/*` 主链路与其直接触达的 WebUI 文案反馈改动，不扩成新的 WebUI 重构。
