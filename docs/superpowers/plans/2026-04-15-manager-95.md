# Manager 9.5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lift manager intelligence, architecture fit, anti-drift stability, offline continuation, and complexity/ROI to a defensible `>=9.5`.

**Architecture:** Keep the existing manager-middle-layer architecture and improve it with three focused changes: stronger default model capability, lower-noise tests, and one more round of state-first code shrink on the manager mainline. No new subsystem, no compatibility shell, no protocol expansion.

**Tech Stack:** TypeScript (ESM), Vitest, pnpm

---

### Task 1: Upgrade Manager Defaults

**Files:**
- Modify: `src/bootstrap/config-default/user-config-defaults.ts`
- Modify: `tests/config-default-loader/defaults-scenarios.ts`
- Modify: `tests/config-default-loader/unknown-keys-scenarios.ts`

- [ ] Write failing tests for manager default model and reasoning effort
- [ ] Run the focused config tests and verify they fail
- [ ] Change the manager defaults to `gpt-5.4` + `high`
- [ ] Re-run the focused config tests and verify they pass

### Task 2: Trim Low-ROI Manager Tests

**Files:**
- Modify: `tests/manager-project-profile-prompt.test.ts`
- Modify: `tests/manager-prompt-capacity-demand.test.ts`
- Modify: `tests/manager-loop-batch-no-reply.test.ts`
- Modify: `tests/manager-task-result-direct-reply.test.ts`
- Modify: `tests/manager-task-contract-validation.test.ts`

- [ ] Replace prompt/reply wording assertions with structure and leakage-boundary assertions where possible
- [ ] Delete or merge assertions that only restate wording
- [ ] Run the focused manager test files and verify they still cover behavior

### Task 3: Shrink One More Manager Mainline Slice

**Files:**
- Modify: `src/policy/manager/task-result-visible-reply.ts`
- Modify: related tests only if behavior-level coverage needs updating

- [ ] Identify one redundant response-shaping path or low-value helper on the manager mainline
- [ ] Write or update a behavior-focused regression test if the change alters observable semantics
- [ ] Implement the minimal shrink without adding a new abstraction shell
- [ ] Run focused tests for the touched slice

### Task 4: Full Verification

**Files:**
- None beyond tracked edits above

- [ ] Run `pnpm run review-code-changes`
- [ ] Inspect `git diff --stat`
- [ ] Re-score the five target dimensions from fresh evidence only
- [ ] Commit the finished change set
