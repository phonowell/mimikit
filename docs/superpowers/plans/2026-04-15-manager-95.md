# Manager 9.5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lift manager intelligence, architecture fit, anti-drift stability, offline continuation, and complexity/ROI to a defensible `>=9.5`.

**Architecture:** Keep the existing manager-middle-layer architecture and improve it with three focused changes: stronger default model capability, lower-noise tests, and one more round of state-first code shrink on the manager mainline. No new subsystem, no compatibility shell, no protocol expansion.

**Tech Stack:** TypeScript (ESM), Vitest, pnpm

---

### Task 1: Upgrade Manager Defaults

**Files:**
- Modify: `src/bootstrap/config-default/user-config-defaults.ts`
- Modify: `tests/config-default-loader/defaults-scenarios.ts`
- Modify: `tests/config-default-loader/unknown-keys-scenarios.ts`

- [x] Confirm the current default is already `gpt-5.4` + `high`; no additional code change needed in this worktree
- [x] Re-run the focused config tests and verify they pass

### Task 2: Trim Low-ROI Manager Tests

**Files:**
- Delete: `tests/manager-reply-normalize.test.ts`
- Delete: `tests/webui-header-status-copy.test.ts`

- [x] Delete low-ROI reply phrasing and static WebUI copy tests
- [x] Keep behavior coverage in manager/action/runtime tests instead of wording tests
- [x] Re-run focused manager coverage after deletion

### Task 3: Shrink One More Manager Mainline Slice

**Files:**
- Modify: `src/policy/manager/manager-turn.ts`
- Modify: `src/policy/manager/task-contract.ts`
- Modify: `src/policy/manager/reply-normalize.ts`
- Modify: `src/policy/manager/loop-batch.ts`
- Modify: `src/policy/manager/loop-batch-reply.ts`
- Modify: `src/policy/manager/loop-batch-run-rounds.ts`
- Modify: `src/policy/manager/loop-batch-run-rounds-diagnostics.ts`
- Delete: `src/foundation/shared/task-draft-canonicalize.ts`
- Delete: `src/policy/manager/reply-normalize-structure.ts`
- Delete: `src/work/project-profile/*`
- Delete: `src/persistence/history/project-profile-events.ts`
- Delete: `src/policy/manager/action-apply-project-profile.ts`
- Delete: `src/policy/manager/action-validation-remember-project-profile.ts`

- [x] Remove the task-draft repair path so invalid drafts fail instead of being compacted into legal contracts
- [x] Remove the structured reply reshaping layer; keep only minimal natural-language cleanup
- [x] Delete the parallel `remember_project_profile` channel and collapse stable repo facts back into `remember_memory`
- [x] Delete the `self-repair` retry shell; invalid action feedback now degrades directly to a clarified stop instead of asking the model to fix its own protocol noise
- [x] Delete the remaining correction-round shell, `action_feedback` prompt packet, `expanded` packet mode, and `maxCorrectionRounds` config knob; keep one validated manager pass plus direct degrade
- [x] Run focused tests for the touched slice

### Task 4: Full Verification

**Files:**
- None beyond tracked edits above

- [x] Run `pnpm run review-code-changes`
- [x] Inspect `git diff --stat`
- [x] Re-score the five target dimensions from fresh evidence only
- [x] Keep the finished change set uncommitted in the requested worktree
